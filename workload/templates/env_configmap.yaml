{{- if .Values.env }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "workload.env-configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
  {{- include "workload.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.env | nindent 2 }}
{{- end }}
