apiVersion: v2
name: workload
description: A Helm chart for Kubernetes

type: application
version: 0.3.1
appVersion: "0.3.1"

dependencies:
  - name: hwl
    version: "0.1.3"
    repository: "file://../hwl"
