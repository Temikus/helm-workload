apiVersion: v2
name: workload
description: A Helm chart for Kubernetes

type: application
version: 0.3.2
appVersion: "0.3.2"

dependencies:
  - name: hwl
    version: "0.1.4"
    repository: "file://../hwl"
