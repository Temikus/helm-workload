apiVersion: v2
name: workload
description: A Helm chart for Kubernetes

type: application
version: 0.3.6
appVersion: "0.3.6"

dependencies:
  - name: hwl
    version: "0.1.8"
    repository: "oci://ghcr.io/temikus/helm-charts"
