apiVersion: v2
name: workload
description: A Helm chart for Kubernetes

type: application
version: 0.3.5
appVersion: "0.3.5"

dependencies:
  - name: hwl
    version: "0.1.7"
    repository: "oci://ghcr.io/temikus/helm-charts"
