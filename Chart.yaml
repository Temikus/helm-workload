apiVersion: v2
name: workload
description: A Helm chart for Kubernetes

type: application
version: 0.3.0
appVersion: "0.3.0"

dependencies:
  - name: hwl
    version: "0.1.1"
    repository: "file://../hwl"
